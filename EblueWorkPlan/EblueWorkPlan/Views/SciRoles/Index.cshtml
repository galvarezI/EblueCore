@model IEnumerable<EblueWorkPlan.Models.SciRole>

@{
    ViewData["Title"] = "Index";
}
<partial name="_Sidebar2" />


<h1>Index</h1>

<p>
    <a class="btn btn-success" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SciRoleName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SciRoleName)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-primary" asp-route-id="@item.SciRolesId">Edit</a> |
                    <a asp-action="Details" class="btn btn-secondary" asp-route-id="@item.SciRolesId">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.SciRolesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
